@model IEnumerable<Game_items_selling_web.Models.Разработчики>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Разработчик)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Дата_основания)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Кол_во_сотрудников)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Разработчик)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Дата_основания)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Кол_во_сотрудников)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Код_разработчика }) |
                @Html.ActionLink("Details", "Details", new { id = item.Код_разработчика }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Код_разработчика })
            </td>
        </tr>
    }

</table>
@Html.ActionLink("Import to Excel", "Excel")