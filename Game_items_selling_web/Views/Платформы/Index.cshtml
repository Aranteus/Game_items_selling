@model IEnumerable<Game_items_selling_web.Models.Платформы>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Платформа)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Платформа)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Код_платформы }) |
            @Html.ActionLink("Details", "Details", new { id=item.Код_платформы }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Код_платформы })
        </td>
    </tr>
}

</table>
